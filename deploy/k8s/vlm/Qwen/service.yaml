apiVersion: v1
kind: Service
metadata:
  name: qwen-server-service
  namespace: vss
  labels:
    app: qwen-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: qwen-server
