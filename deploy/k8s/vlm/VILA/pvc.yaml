apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vila-ngc-model-cache-pvc
  namespace: vss
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: gpfs