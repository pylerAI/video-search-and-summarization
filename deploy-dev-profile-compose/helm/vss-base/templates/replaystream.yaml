{{- if .Values.replaystreamMs.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vss-base.fullname" . }}-replaystream-ms
  labels:
    {{- include "vss-base.labels" . | nindent 4 }}
    app.kubernetes.io/component: replaystream-ms
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: replaystream-ms
  template:
    metadata:
      labels:
        app.kubernetes.io/component: replaystream-ms
    spec:
      {{- include "vss-base.imagePullSecrets" . | nindent 6 }}
      {{- if .Values.replaystreamMs.runtimeClassName }}
      runtimeClassName: {{ .Values.replaystreamMs.runtimeClassName }}
      {{- end }}
      containers:
        - name: replaystream-ms
          image: "{{ .Values.replaystreamMs.image.repository }}:{{ .Values.replaystreamMs.image.tag }}"
          imagePullPolicy: {{ .Values.replaystreamMs.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.replaystreamMs.port }}
              name: http
          env:
            - name: NVIDIA_VISIBLE_DEVICES
              value: "all"
            - name: ADAPTOR
              value: "replaystream-ms"
            - name: HTTP_PORT
              value: {{ .Values.replaystreamMs.port | quote }}
            - name: STORAGE_MODULE_ENDPOINT
              value: "http://{{ include "vss-base.fullname" . }}-storage-ms:{{ .Values.storageMs.port }}"
            - name: SENSOR_MODULE_ENDPOINT
              value: "http://{{ include "vss-base.fullname" . }}-sensor-ms:{{ .Values.sensorMs.port }}"
            - name: REDIS_HOSTADDR
              value: "{{ include "vss-base.fullname" . }}-redis"
            - name: REDIS_PORT
              value: {{ .Values.redis.port | quote }}
            - name: REDIS_MSG_KEY
              value: "vst.event"
          volumeMounts:
            - name: vst-data
              mountPath: /home/vst/vst_release/vst_data
            - name: vst-video
              mountPath: /home/vst/vst_release/vst_video
            - name: vst-clips
              mountPath: /home/vst/vst_release/streamer_videos
          {{- with .Values.replaystreamMs.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: vst-data
          {{- if .Values.vstSharedVolumes.vstData.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "vss-base.fullname" . }}-vst-data
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: vst-video
          {{- if .Values.vstSharedVolumes.vstVideo.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "vss-base.fullname" . }}-vst-video
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: vst-clips
          {{- if .Values.vstSharedVolumes.vstClips.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "vss-base.fullname" . }}-vst-clips
          {{- else }}
          emptyDir: {}
          {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "vss-base.fullname" . }}-replaystream-ms
  labels:
    {{- include "vss-base.labels" . | nindent 4 }}
    app.kubernetes.io/component: replaystream-ms
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.replaystreamMs.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: replaystream-ms
{{- end }}
